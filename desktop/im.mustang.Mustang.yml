id: im.mustang.Mustang
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: mustang

finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --share=network
  - --device=dri
  - --socket=pulseaudio
  - --device=all
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.kde.StatusNotifierWatcher
  - --filesystem=xdg-download
  - --env=ELECTRON_OZONE_PLATFORM_HINT=auto

modules:
  - name: mustang
    buildsystem: simple
    build-commands:
      - ar x mustang_0.9.30_arm64.deb
    sources:
      - type: file
        url: https://github.com/mustang-im/mustang/releases/download/v0.9.30/mustang_0.9.30_arm64.deb
        dest-filename: mustang_0.9.30_arm64.deb
        only-arches:
          - arm64
